# Assets

此目录用于存放项目相关数字原始资产。

建议分类：

- `design/`：设计稿、视觉规范、交互原型。
- `references/`：参考图、参考视频、参考音频。
- `product/`：产品展示素材、说明配图。
- `exports/`：可提交的导出样例。

不应提交运行时用户上传文件、缓存文件、临时生成文件。此类文件应存放在 `.gitignore` 已过滤的目录中，例如 `public/uploads/`。

