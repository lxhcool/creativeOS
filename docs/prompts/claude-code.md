# Claude Code Instructions

You are building Creative OS according to this spec.

Rules:

- Use TypeScript.
- Do not use `any` unless explicitly justified.
- Schema-first development.
- Do not call model providers directly from business logic.
- Use Model Gateway.
- Use AgentProvider abstraction.
- Keep UI implementation separate from business logic.
- First implement Auth, Provider Center, Project System, Canvas, Node System, MVP Game Asset workflow.
